{
  "name": "shrimp-fried-this-rice",
  "version": "1.0.0",
  "description": "",
  "main": "./src/index.ts",
  "engines": {
    "node": ">=23.6.0"
  },
  "private": true,
  "scripts": {
    "start": "node ./src/index.ts",
    "deploycommands": "node ./src/deploy-commands.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --watch ./src/index.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "zip": "zip -X -r project-export.zip . -x 'node_modules/*' -x '.git/*' -x 'dist/*' -x '.env' -x '*.zip' -x '.DS_Store' -x '__MACOSX/*'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "better-sqlite3": "^13.0.3",
    "discord.js": "^14.27.0",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^9.6.0",
    "@types/node": "^26.4.0",
    "typescript": "^7.0.2"
  }
}
